export class No {
    source: string;
    target: string;
    type: string;
        constructor (
            source: string,
            target: string,
            type: string) {
                this.source = source;
                this.target = target;
                this.type = type;
        }
    }